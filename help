#include <stdio.h>


int main() {
    int n, m, num = 0;
    scanf("%d", &n);
    scanf("%d", &m);
    int matrix[n][m];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            scanf("%d", &num);
            matrix[i][j] = num;
        }
    }
    for (int i = 1; i < n; i++){
        
    }
}
